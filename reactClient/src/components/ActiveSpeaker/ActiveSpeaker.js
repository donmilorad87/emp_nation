
/* import { useActiveSpeakerId } from '@daily-co/daily-react'; */

export default async function CurrentSpeaker() {
 /*  const activeSpeakerId = useActiveSpeakerId();

  
  return <div>Current speaker id: {activeSpeakerId ?? 'none'}</div>; */
};